import { createSlice } from "@reduxjs/toolkit";

export const likeSlice = createSlice({
    name: 'like',
    initialState:{
        like: 0
    },
    reducers: {
        like: (state, action) => {
            s
        }
    }
})